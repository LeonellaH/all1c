&НаКлиенте
Перем СцТ_ГлавнаяФорма Экспорт;


&НаКлиенте
Процедура ВернутьсяВГлавноеОкно(Команда)
	
	Закрыть();
	
КонецПроцедуры

&НаКлиенте
Процедура НовыйСценарий(Команда)
	
	Закрыть();
	СцТ_ГлавнаяФорма.ОбработкаКомандыЗапускаТестируемогоПриложения();
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьДляРедактирования(Команда)
	
	Закрыть();
	СцТ_ГлавнаяФорма.СцТ_ВыполнитьОткрытиеСценария("Редактирование");
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьДляВыполнения(Команда)
	
	Закрыть();
	СцТ_ГлавнаяФорма.СцТ_ВыполнитьОткрытиеСценария("Выполнение");
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия = "СцТ_ПринудительноеЗакрытиеВсехФормОбработки" Тогда
		Модифицированность = Ложь;
		Если Открыта() Тогда
			Закрыть();
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры


